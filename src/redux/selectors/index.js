export const authState$ = (state) => state.auth;

export const msgState$ = (state) => state.notifications.message;

export const empState$ = (state) => state.employee;

export const serviceCateState$ = (state) => state.serviceCate;

export const serviceState$ = (state) => state.service;

export const customerState$ = (state) => state.customer;

export const serviceCustomerState$ = (state) => state.serviceCustomer;

export const reportState$ = (state) => state.report;